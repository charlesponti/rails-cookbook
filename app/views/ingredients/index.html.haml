%table.table{style:'width: 60%; margin: auto'}
  %thead
    %th{colspan: 3, style:'text-align:center'}
      %h1 Ingredients
  %tr
    %th Name
    %th Pic
    %th

  - @ingredients.each do |ingredient|
    %tr
      %td= link_to ingredient.name, ingredient
      %td= ingredient.pic
      %td
        = link_to 'Edit', edit_ingredient_path(ingredient), class: 'btn'
        = link_to 'Destroy', ingredient, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-danger'
  %tr
    %td{colspan: 3, style:'text-align:center'}
      = link_to 'New Ingredient', new_ingredient_path, class: 'btn'
